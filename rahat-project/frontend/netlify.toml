# App-level Netlify config (used when base directory = rahat-project/frontend)
[build]
  command = "npm ci && npm run build"

[build.environment]
  NODE_VERSION = "20"
  NPM_CONFIG_PRODUCTION = "false"

[[plugins]]
  package = "@netlify/plugin-nextjs"
